function toNumber(value){if(value===null||value===undefined||value==="")return "";var number=Number(String(value).replace(",","."));return isNaN(number)?"":number}
function getSpreadsheet(){return SpreadsheetApp.getActiveSpreadsheet()}
function ensureEntriesSheet(){var sheet=getSpreadsheet().getSheetByName("Einträge");if(!sheet)sheet=getSpreadsheet().insertSheet("Einträge");var header=["Erfasst am","Zeitraum von","Zeitraum bis","Fahrer","Fahrer-Stundensatz","Fahrer-Kosten","Zugmaschine","Anbaugerät","Geräte-Kombination","Abrechnungsmodus","Abrechnungseinheit","Menge","Preis pro Einheit","Zugmaschinenkosten","Anbaugerätkosten","Geräte-/Maschinenkosten","Stundenzähler Beginn","Stundenzähler Ende","Betriebsstunden","Arbeitszeit Fahrer Stunden","Gesamtkosten","Diesel Liter","Einsatzart","Bemerkung","Lieferschein Nummer","Kunde","Kundenadresse","Kundenort","Kundenkontakt"];if(sheet.getLastRow()===0){sheet.appendRow(header)}else{sheet.getRange(1,1,1,header.length).setValues([header])}sheet.setFrozenRows(1);return sheet}
function ensureItemsSheet(){var sheet=getSpreadsheet().getSheetByName("Geräte");if(!sheet)sheet=getSpreadsheet().insertSheet("Geräte");var header=["Typ","Name","Einheit","Preis","Abrechnungsmodus","Aktiv"];if(sheet.getLastRow()===0){sheet.appendRow(header)}else{sheet.getRange(1,1,1,header.length).setValues([header])}sheet.setFrozenRows(1);return sheet}
function ensureCompanySheet(){var sheet=getSpreadsheet().getSheetByName("Firmendaten");if(!sheet)sheet=getSpreadsheet().insertSheet("Firmendaten");if(sheet.getLastRow()===0){sheet.appendRow(["Feld","Wert"]);sheet.appendRow(["name","Max Mustermann"]);sheet.appendRow(["zusatz",""]);sheet.appendRow(["adresse","Musterstraße 1"]);sheet.appendRow(["ort","1234 Musterstadt"]);sheet.appendRow(["telefon","0664 / 12345678"]);sheet.appendRow(["email","office@muster.at"]);sheet.appendRow(["uid",""])}else{sheet.getRange(1,1,1,2).setValues([["Feld","Wert"]])}sheet.setFrozenRows(1);return sheet}
function getCompany(){var sheet=ensureCompanySheet(),lastRow=sheet.getLastRow(),company={name:"Max Mustermann",zusatz:"",adresse:"Musterstraße 1",ort:"1234 Musterstadt",telefon:"0664 / 12345678",email:"office@muster.at",uid:""};if(lastRow<2)return company;sheet.getRange(2,1,lastRow-1,2).getValues().forEach(function(row){var key=String(row[0]||"").trim();if(key)company[key]=String(row[1]||"")});return company}
function saveCompany(company){var sheet=ensureCompanySheet(),rows=[["Feld","Wert"],["name",company.name||""],["zusatz",company.zusatz||""],["adresse",company.adresse||""],["ort",company.ort||""],["telefon",company.telefon||""],["email",company.email||""],["uid",company.uid||""]];sheet.clear();sheet.getRange(1,1,rows.length,2).setValues(rows);sheet.setFrozenRows(1)}
function ensureSummarySheet(){var sheet=getSpreadsheet().getSheetByName("Auswertung");if(!sheet)sheet=getSpreadsheet().insertSheet("Auswertung");sheet.clear();sheet.getRange("A1").setValue("Auswertung Maschinen-App").setFontWeight("bold").setFontSize(16);sheet.getRange("A3").setValue("Übersicht pro Zugmaschine").setFontWeight("bold");sheet.getRange("A4").setFormula('=QUERY(Einträge!A:AC;"select G, sum(O), sum(S), sum(V) where G is not null group by G label G \'Zugmaschine\', sum(O) \'Betriebsstunden\', sum(S) \'Kosten\', sum(V) \'Diesel Liter\'";1)');sheet.getRange("A15").setValue("Übersicht pro Anbaugerät").setFontWeight("bold");sheet.getRange("A16").setFormula('=QUERY(Einträge!A:AC;"select H, J, sum(K), sum(S) where H is not null group by H,J label H \'Anbaugerät\', J \'Einheit\', sum(K) \'Menge\', sum(S) \'Kosten\'";1)');sheet.getRange("A30").setValue("Übersicht pro Kunde").setFontWeight("bold");sheet.getRange("A31").setFormula('=QUERY(Einträge!A:AC;"select Z, sum(K), sum(S), sum(F), sum(U) where Z is not null group by Z label Z \'Kunde\', sum(K) \'Menge\', sum(S) \'Gerätekosten\', sum(F) \'Fahrerkosten\', sum(U) \'Gesamtkosten\'";1)')}
function setupSheets(){ensureEntriesSheet();ensureItemsSheet();ensureCompanySheet();ensureSummarySheet()}
function makeDeliveryNoteNumber(){var year=new Date().getFullYear(),sheet=ensureEntriesSheet(),lastRow=sheet.getLastRow(),count=0;if(lastRow>=2){sheet.getRange(2,25,lastRow-1,1).getValues().forEach(function(row){if(String(row[0]||"").indexOf("LS-"+year+"-")===0)count++})}return"LS-"+year+"-"+String(count+1).padStart(4,"0")}
function getLastDeliveryNoteNumber(){var sheet=ensureEntriesSheet(),lastRow=sheet.getLastRow();if(lastRow<2)return"";return sheet.getRange(lastRow,25).getValue()}
function getItemsByType(type){var sheet=ensureItemsSheet(),lastRow=sheet.getLastRow();if(lastRow<2)return[];var list=[];sheet.getRange(2,1,lastRow-1,6).getValues().forEach(function(row){var typ=String(row[0]||"").trim(),name=String(row[1]||"").trim(),unit=String(row[2]||"h").trim(),rate=toNumber(row[3]),mode=String(row[4]||"stunden").trim(),active=String(row[5]||"Ja").trim().toLowerCase();if(typ===type&&name&&active!=="nein"&&active!=="false"&&active!=="0")list.push({name:name,unit:unit||"h",rate:rate===""?0:rate,mode:mode||"stunden"})});list.sort(function(a,b){return a.name.localeCompare(b.name)});return list}
function addMachine(type,name,rate,unit,mode){var sheet=ensureItemsSheet(),itemType=String(type||"").trim(),itemName=String(name||"").trim();if(!itemType||!itemName)return;var itemRate=toNumber(rate);if(itemRate==="")itemRate=0;sheet.appendRow([itemType,itemName,unit||"h",itemRate,mode||"stunden","Ja"]);ensureSummarySheet()}
function deleteMachine(type,name){var sheet=ensureItemsSheet(),lastRow=sheet.getLastRow();for(var row=2;row<=lastRow;row++){if(String(sheet.getRange(row,1).getValue()).trim()===String(type).trim()&&String(sheet.getRange(row,2).getValue()).trim()===String(name).trim()){sheet.getRange(row,6).setValue("Nein");ensureSummarySheet();return}}}
function findLastEndeForZugmaschine(zugmaschine){var sheet=ensureEntriesSheet(),lastRow=sheet.getLastRow();if(lastRow<2)return"";for(var row=lastRow;row>=2;row--){if(String(sheet.getRange(row,7).getValue()).trim()===zugmaschine)return sheet.getRange(row,14).getValue()}return""}
function doPost(e){setupSheets();var data=JSON.parse(e.postData.contents),action=data.action||"entry";if(action==="saveCompany"){saveCompany(data.company||{});return ContentService.createTextOutput(JSON.stringify({ok:true})).setMimeType(ContentService.MimeType.JSON)}if(action==="addMachine"){addMachine(data.typ,data.name||data.maschine,data.stundensatz,data.einheit,data.modus);return ContentService.createTextOutput(JSON.stringify({ok:true})).setMimeType(ContentService.MimeType.JSON)}if(action==="deleteMachine"){deleteMachine(data.typ,data.name||data.maschine);return ContentService.createTextOutput(JSON.stringify({ok:true})).setMimeType(ContentService.MimeType.JSON)}var sheet=ensureEntriesSheet(),start=toNumber(data.stundenStart),ende=toNumber(data.stundenEnde),hours=toNumber(data.betriebsstunden),quantity=toNumber(data.menge),price=toNumber(data.preisProEinheit),driverRate=toNumber(data.fahrerStundensatz),driverHours=toNumber(data.fahrerArbeitszeitStunden),diesel=toNumber(data.diesel);if(driverRate==="")driverRate=0;if(driverHours==="")driverHours=0;var driverCost=Math.round(driverRate*driverHours*100)/100;if(quantity==="")quantity=hours;if(price==="")price=0;var machineCost=toNumber(data.maschinenKosten);if(machineCost===""&&quantity!==""&&price!=="")machineCost=Math.round(quantity*price*100)/100;var totalCost=toNumber(data.gesamtkosten);if(totalCost===""&&machineCost!=="")totalCost=Math.round((machineCost+driverCost)*100)/100;var lsNumber=makeDeliveryNoteNumber();sheet.appendRow([new Date(),data.datumVon||"",data.datumBis||"",data.fahrer||"",driverRate,driverCost,data.zugmaschine||"",data.anbaugeraet||"",data.geraetKombination||"",data.abrechnungsmodus||"",data.einheit||"h",quantity,price,toNumber(data.zugKosten),toNumber(data.anbauKosten),machineCost,start,ende,hours,driverHours,totalCost,diesel,data.einsatzart==="ueberbetrieblich"?"überbetrieblich":"innerbetrieblich",data.bemerkung||"",lsNumber,data.kundeName||"",data.kundeAdresse||"",data.kundeOrt||"",data.kundeKontakt||""];ensureSummarySheet();return ContentService.createTextOutput(JSON.stringify({ok:true,lsNumber:lsNumber})).setMimeType(ContentService.MimeType.JSON)}
function doGet(e){setupSheets();var action=e&&e.parameter?e.parameter.action:"";if(action==="company")return ContentService.createTextOutput(JSON.stringify({ok:true,company:getCompany()})).setMimeType(ContentService.MimeType.JSON);if(action==="machines")return ContentService.createTextOutput(JSON.stringify({ok:true,zugmaschinen:getItemsByType("zugmaschine"),anbaugeraete:getItemsByType("anbaugeraet")})).setMimeType(ContentService.MimeType.JSON);if(action==="last")return ContentService.createTextOutput(JSON.stringify({ok:true,lastEnde:findLastEndeForZugmaschine(e.parameter.zugmaschine||e.parameter.machine||"")})).setMimeType(ContentService.MimeType.JSON);if(action==="lastDeliveryNote")return ContentService.createTextOutput(JSON.stringify({ok:true,lsNumber:getLastDeliveryNoteNumber()})).setMimeType(ContentService.MimeType.JSON);return ContentService.createTextOutput("Maschinen-App ist aktiv.").setMimeType(ContentService.MimeType.TEXT)}
