import React,{useEffect,useMemo,useState}from"react";
const APP_NAME="Maschinen",SHEET_WEBAPP_URL=import.meta.env.VITE_GOOGLE_SCRIPT_URL||"";
const DEFAULT_COMPANY={name:"Max Mustermann",zusatz:"",adresse:"Musterstraße 1",ort:"1234 Musterstadt",telefon:"0664 / 12345678",email:"office@muster.at",uid:""};
function parseDecimal(v){if(v===null||v===undefined)return NaN;const c=String(v).trim().replace(/\s/g,"").replace(",",".");return c===""?NaN:Number(c)}
function formatEuro(v){if(v===""||Number.isNaN(v)||v===null||v===undefined)return"—";return"€ "+Number(v).toLocaleString("de-AT",{minimumFractionDigits:2,maximumFractionDigits:2})}
function formatHours(v){if(v===""||Number.isNaN(v)||v===null||v===undefined)return"—";return Number(v).toLocaleString("de-AT",{minimumFractionDigits:1,maximumFractionDigits:2})+" h"}
function formatNumber(v){if(v===""||Number.isNaN(v)||v===null||v===undefined)return"—";return Number(v).toLocaleString("de-AT",{minimumFractionDigits:1,maximumFractionDigits:2})}
function unitLabel(u){return u==="ha"?"ha":u==="m3"?"m³":"h"}function unitName(u){return u==="ha"?"Hektar":u==="m3"?"m³":"Stunden"}
function pad2(v){return String(v).padStart(2,"0")}function secondsToClock(t){const s=Math.max(0,Math.floor(t||0)),h=Math.floor(s/3600),m=Math.floor((s%3600)/60),sec=s%60;return`${pad2(h)}:${pad2(m)}:${pad2(sec)}`}
function esc(v){return String(v??"").replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;")}
function Field({label,children}){return <label className="field"><span>{label}</span>{children}</label>}
export default function App(){const today=new Date().toISOString().slice(0,10);const[form,setForm]=useState({datumVon:today,datumBis:today,fahrer:"",fahrerStundensatz:"",zugmaschine:"",anbaugeraet:"",stundenStart:"",stundenEnde:"",menge:"",diesel:"",einsatzart:"innerbetrieblich",arbeitszeitStunden:"",kundeName:"",kundeAdresse:"",kundeOrt:"",kundeKontakt:"",bemerkung:""});
const[company,setCompany]=useState(DEFAULT_COMPANY),[companyDraft,setCompanyDraft]=useState(DEFAULT_COMPANY),[showSettings,setShowSettings]=useState(false),[zugmaschinen,setZugmaschinen]=useState([]),[anbaugeraete,setAnbaugeraete]=useState([]),[newZugmaschine,setNewZugmaschine]=useState(""),[newZugRate,setNewZugRate]=useState(""),[newAnbaugeraet,setNewAnbaugeraet]=useState(""),[newAnbauRate,setNewAnbauRate]=useState(""),[newAnbauUnit,setNewAnbauUnit]=useState("h"),[message,setMessage]=useState(""),[sending,setSending]=useState(false),[loading,setLoading]=useState(false),[showManage,setShowManage]=useState(false),[stopwatchSeconds,setStopwatchSeconds]=useState(0),[stopwatchRunning,setStopwatchRunning]=useState(false),[lastDeliveryNote,setLastDeliveryNote]=useState(null);
useEffect(()=>{loadInitialData()},[]);useEffect(()=>{if(!stopwatchRunning)return;const timer=setInterval(()=>setStopwatchSeconds(c=>c+1),1000);return()=>clearInterval(timer)},[stopwatchRunning]);
const selectedZugmaschine=zugmaschinen.find(i=>i.name===form.zugmaschine),selectedAnbaugeraet=anbaugeraete.find(i=>i.name===form.anbaugeraet),anbauUnit=selectedAnbaugeraet?.unit||"h",isQuantity=!!selectedAnbaugeraet&&anbauUnit!=="h";
const zugRate=selectedZugmaschine?Number(selectedZugmaschine.rate||0):0,anbauRate=selectedAnbaugeraet?Number(selectedAnbaugeraet.rate||0):0,startNumber=parseDecimal(form.stundenStart),endNumber=parseDecimal(form.stundenEnde),mengeNumber=parseDecimal(form.menge),dieselNumber=parseDecimal(form.diesel),manualDriverHours=parseDecimal(form.arbeitszeitStunden),driverRateNumber=parseDecimal(form.fahrerStundensatz);
const gefahreneStunden=useMemo(()=>{const s=parseDecimal(form.stundenStart),e=parseDecimal(form.stundenEnde);if(Number.isNaN(s)||Number.isNaN(e))return"";return Math.round((e-s)*100)/100},[form.stundenStart,form.stundenEnde]);
const hasValidHours=gefahreneStunden!==""&&gefahreneStunden>=0,hasValidQuantity=!Number.isNaN(mengeNumber)&&mengeNumber>=0,billingQuantity=isQuantity?mengeNumber:(hasValidHours?Number(gefahreneStunden):""),billingUnit=isQuantity?anbauUnit:"h",billingRate=isQuantity?anbauRate:(zugRate+anbauRate),machineCost=(isQuantity?hasValidQuantity:hasValidHours)?Math.round(Number(billingQuantity)*billingRate*100)/100:"",driverHoursFromStopwatch=Math.round((stopwatchSeconds/3600)*100)/100,driverHours=!Number.isNaN(manualDriverHours)?manualDriverHours:driverHoursFromStopwatch,driverRate=Number.isNaN(driverRateNumber)?0:driverRateNumber,driverCost=Math.round(driverHours*driverRate*100)/100,totalCost=machineCost===""?"":Math.round((Number(machineCost)+driverCost)*100)/100,equipmentName=isQuantity?form.anbaugeraet:[form.zugmaschine,form.anbaugeraet].filter(Boolean).join(" + ");
const canSave=form.datumVon&&form.datumBis&&form.fahrer.trim()&&form.kundeName.trim()&&(isQuantity?form.anbaugeraet&&hasValidQuantity:form.zugmaschine&&!Number.isNaN(startNumber)&&!Number.isNaN(endNumber)&&hasValidHours)&&!sending;
async function apiGet(action,extra=""){const r=await fetch(`${SHEET_WEBAPP_URL}?action=${action}${extra}&ts=${Date.now()}`);return await r.json()}async function apiPost(payload){await fetch(SHEET_WEBAPP_URL,{method:"POST",mode:"no-cors",headers:{"Content-Type":"text/plain;charset=utf-8"},body:JSON.stringify(payload)})}
async function loadInitialData(){await loadMachines();await loadCompany()}async function loadCompany(){if(!SHEET_WEBAPP_URL)return;try{const d=await apiGet("company");if(d.ok&&d.company){const c={...DEFAULT_COMPANY,...d.company};setCompany(c);setCompanyDraft(c)}}catch{}}
async function saveCompany(){if(!companyDraft.name.trim()){alert("Bitte Firmenname eingeben.");return}setLoading(true);try{await apiPost({action:"saveCompany",company:companyDraft});setCompany(companyDraft);setShowSettings(false);setMessage("Firmendaten wurden gespeichert.")}catch{setMessage("Firmendaten konnten nicht gespeichert werden.")}finally{setLoading(false)}}
async function loadMachines(){setMessage("");if(!SHEET_WEBAPP_URL){setMessage("Google-Script-Link fehlt noch. Bitte VITE_GOOGLE_SCRIPT_URL in Vercel eintragen.");return}setLoading(true);try{const d=await apiGet("machines");setZugmaschinen((Array.isArray(d.zugmaschinen)?d.zugmaschinen:[]).map(i=>({name:String(i.name||"").trim(),rate:Number(i.rate||0),unit:i.unit||"h"})).filter(i=>i.name));setAnbaugeraete((Array.isArray(d.anbaugeraete)?d.anbaugeraete:[]).map(i=>({name:String(i.name||"").trim(),rate:Number(i.rate||0),unit:i.unit||"h"})).filter(i=>i.name))}catch{setMessage("Maschinen konnten nicht geladen werden. Bitte Google-Script prüfen.")}finally{setLoading(false)}}
async function takeLastValue(){if(!form.zugmaschine){alert("Bitte zuerst eine Zugmaschine auswählen.");return}setLoading(true);try{const d=await apiGet("last",`&zugmaschine=${encodeURIComponent(form.zugmaschine)}`);if(!d.ok||d.lastEnde===""||d.lastEnde==null){setMessage(`Für ${form.zugmaschine} wurde noch kein letzter Stundenzähler gefunden.`);return}const last=String(d.lastEnde).replace(".",",");setForm(c=>({...c,stundenStart:last}));setMessage(`Letzter Stundenzähler für ${form.zugmaschine} übernommen: ${last}`)}catch{setMessage("Letzter Wert konnte nicht geladen werden.")}finally{setLoading(false)}}
async function addZugmaschine(){const name=newZugmaschine.trim(),rate=parseDecimal(newZugRate);if(!name||Number.isNaN(rate)||rate<0){alert("Bitte Zugmaschine und gültigen Preis eingeben.");return}setLoading(true);try{await apiPost({action:"addMachine",typ:"zugmaschine",name,stundensatz:rate,einheit:"h"});setZugmaschinen([...zugmaschinen,{name,rate,unit:"h"}].sort((a,b)=>a.name.localeCompare(b.name,"de")));setNewZugmaschine("");setNewZugRate("");setForm(c=>({...c,zugmaschine:name}));setMessage(`Zugmaschine hinzugefügt: ${name}`)}catch{setMessage("Zugmaschine konnte nicht hinzugefügt werden.")}finally{setLoading(false)}}
async function addAnbaugeraet(){const name=newAnbaugeraet.trim(),rate=parseDecimal(newAnbauRate);if(!name||Number.isNaN(rate)||rate<0){alert("Bitte Anbaugerät und gültigen Preis eingeben.");return}setLoading(true);try{await apiPost({action:"addMachine",typ:"anbaugeraet",name,stundensatz:rate,einheit:newAnbauUnit});setAnbaugeraete([...anbaugeraete,{name,rate,unit:newAnbauUnit}].sort((a,b)=>a.name.localeCompare(b.name,"de")));setNewAnbaugeraet("");setNewAnbauRate("");setNewAnbauUnit("h");setForm(c=>({...c,anbaugeraet:name,menge:""}));setMessage(`Anbaugerät hinzugefügt: ${name}`)}catch{setMessage("Anbaugerät konnte nicht hinzugefügt werden.")}finally{setLoading(false)}}
async function deleteItem(typ,name){if(!confirm(`Wirklich löschen/deaktivieren?\n\n${name}`))return;setLoading(true);try{await apiPost({action:"deleteMachine",typ,name});if(typ==="zugmaschine"){const u=zugmaschinen.filter(i=>i.name!==name);setZugmaschinen(u);if(form.zugmaschine===name)setForm(c=>({...c,zugmaschine:u[0]?.name||""}))}else{const u=anbaugeraete.filter(i=>i.name!==name);setAnbaugeraete(u);if(form.anbaugeraet===name)setForm(c=>({...c,anbaugeraet:"",menge:""}))}setMessage(`Gelöscht: ${name}`)}catch{setMessage("Löschen fehlgeschlagen.")}finally{setLoading(false)}}
function useStopwatchAsWorkTime(){setForm(c=>({...c,arbeitszeitStunden:String(Math.round((stopwatchSeconds/3600)*100)/100).replace(".",",")}))}function resetStopwatch(){setStopwatchRunning(false);setStopwatchSeconds(0)}
function buildDeliveryNoteData(lsNumber="VORANSICHT"){return{lsNumber,datum:new Date().toLocaleDateString("de-AT"),dienstleister:company,kunde:{name:form.kundeName.trim(),adresse:form.kundeAdresse.trim(),ort:form.kundeOrt.trim(),kontakt:form.kundeKontakt.trim()},einsatz:{datumVon:form.datumVon,datumBis:form.datumBis,fahrer:form.fahrer.trim(),zugmaschine:isQuantity?"":form.zugmaschine,anbaugeraet:form.anbaugeraet,position:equipmentName,einsatzart:form.einsatzart==="ueberbetrieblich"?"überbetrieblich":"innerbetrieblich",stundenStart:isQuantity?"":startNumber,stundenEnde:isQuantity?"":endNumber,menge:billingQuantity,einheit:billingUnit,preis:billingRate,maschinenkosten:machineCost,fahrerStundensatz:driverRate,fahrerArbeitszeit:Number.isNaN(driverHours)?0:driverHours,fahrerkosten:driverCost,gesamtkosten:totalCost,diesel:Number.isNaN(dieselNumber)?0:dieselNumber,bemerkung:form.bemerkung.trim()}}}
function openDeliveryNote(data){const d=data;const html=`<!doctype html><html lang="de"><head><meta charset="utf-8"/><title>${esc(d.lsNumber)} Lieferschein</title><style>body{font-family:Arial,sans-serif;color:#111827;margin:0;padding:32px;background:#fff}.page{max-width:850px;margin:0 auto}.top{display:flex;justify-content:space-between;gap:30px;border-bottom:3px solid #111827;padding-bottom:18px}h1{font-size:34px;margin:0 0 8px}h2{font-size:16px;margin:28px 0 8px;border-bottom:1px solid #d1d5db;padding-bottom:6px}p{margin:3px 0;line-height:1.35}.muted{color:#6b7280}.box{border:1px solid #d1d5db;border-radius:10px;padding:14px;margin-top:10px}table{width:100%;border-collapse:collapse;margin-top:16px}th,td{border:1px solid #d1d5db;padding:10px;text-align:left;font-size:14px}th{background:#f3f4f6}.right{text-align:right}.signatures{display:grid;grid-template-columns:1fr 1fr;gap:60px;margin-top:70px}.line{border-top:1px solid #111827;padding-top:8px;text-align:center}@media print{body{padding:0}.page{max-width:none}.no-print{display:none}}</style></head><body><div class="page"><div class="top"><div><h1>LIEFERSCHEIN</h1><p><strong>Nr.:</strong> ${esc(d.lsNumber)}</p><p><strong>Datum:</strong> ${esc(d.datum)}</p></div><div><p><strong>${esc(d.dienstleister.name)}</strong></p><p>${esc(d.dienstleister.zusatz)}</p><p>${esc(d.dienstleister.adresse)}</p><p>${esc(d.dienstleister.ort)}</p><p>${esc(d.dienstleister.telefon)}</p><p>${esc(d.dienstleister.email)}</p><p>${esc(d.dienstleister.uid)}</p></div></div><h2>Kunde / Auftraggeber</h2><div class="box"><p><strong>${esc(d.kunde.name)}</strong></p><p>${esc(d.kunde.adresse)}</p><p>${esc(d.kunde.ort)}</p><p>${esc(d.kunde.kontakt)}</p></div><h2>Einsatzdaten</h2><p><strong>Zeitraum:</strong> ${esc(d.einsatz.datumVon)} bis ${esc(d.einsatz.datumBis)}</p><p><strong>Fahrer:</strong> ${esc(d.einsatz.fahrer)}</p>${d.einsatz.zugmaschine?`<p><strong>Zugmaschine:</strong> ${esc(d.einsatz.zugmaschine)}</p>`:""}<p><strong>Anbaugerät:</strong> ${esc(d.einsatz.anbaugeraet||"-")}</p><p><strong>Einsatzart:</strong> ${esc(d.einsatz.einsatzart)}</p><table><thead><tr><th>Position</th><th class="right">Menge</th><th>Einheit</th><th class="right">Preis</th><th class="right">Gesamt</th></tr></thead><tbody><tr><td>${esc(d.einsatz.position)}</td><td class="right">${esc(formatNumber(d.einsatz.menge))}</td><td>${esc(unitLabel(d.einsatz.einheit))}</td><td class="right">${esc(formatEuro(d.einsatz.preis))}/${esc(unitLabel(d.einsatz.einheit))}</td><td class="right">${esc(formatEuro(d.einsatz.maschinenkosten))}</td></tr><tr><td>Fahrer-Arbeitszeit</td><td class="right">${esc(formatHours(d.einsatz.fahrerArbeitszeit))}</td><td>h</td><td class="right">${esc(formatEuro(d.einsatz.fahrerStundensatz))}</td><td class="right">${esc(formatEuro(d.einsatz.fahrerkosten))}</td></tr><tr><td colspan="4" class="right"><strong>Gesamt</strong></td><td class="right"><strong>${esc(formatEuro(d.einsatz.gesamtkosten))}</strong></td></tr></tbody></table><h2>Weitere Angaben</h2>${d.einsatz.stundenStart!==""?`<p><strong>Stundenzähler Beginn:</strong> ${esc(d.einsatz.stundenStart)}</p><p><strong>Stundenzähler Ende:</strong> ${esc(d.einsatz.stundenEnde)}</p>`:""}<p><strong>Diesel:</strong> ${esc(d.einsatz.diesel)} Liter</p><p><strong>Bemerkung:</strong> ${esc(d.einsatz.bemerkung)}</p><div class="signatures"><div class="line">Unterschrift Kunde</div><div class="line">Unterschrift Fahrer</div></div><p class="muted no-print" style="margin-top:40px">Zum Speichern als PDF bitte Drucken öffnen und „Als PDF sichern“ wählen.</p></div><script>window.onload=()=>setTimeout(()=>window.print(),300)</script></body></html>`;const win=window.open("","_blank");if(!win){alert("Popup wurde blockiert. Bitte Popups erlauben.");return}win.document.open();win.document.write(html);win.document.close()}
function previewDeliveryNote(){if(!form.kundeName.trim()){alert("Bitte Kunden eingeben.");return}if(!form.fahrer.trim()){alert("Bitte Fahrer eingeben.");return}if(isQuantity&&!hasValidQuantity){alert("Bitte Menge eingeben.");return}if(!isQuantity&&!hasValidHours){alert("Bitte gültige Betriebsstunden eingeben.");return}openDeliveryNote(buildDeliveryNoteData())}
async function saveEntry(e){e.preventDefault();setMessage("");setLastDeliveryNote(null);if(!SHEET_WEBAPP_URL){setMessage("Google-Script-Link fehlt noch.");return}if(!canSave){alert("Bitte alle Pflichtfelder richtig ausfüllen.");return}setSending(true);const previewData=buildDeliveryNoteData();const payload={action:"entry",datumVon:form.datumVon,datumBis:form.datumBis,fahrer:form.fahrer.trim(),fahrerStundensatz:driverRate,fahrerkosten:driverCost,zugmaschine:isQuantity?"":form.zugmaschine,anbaugeraet:form.anbaugeraet,geraetKombination:equipmentName,einheit:billingUnit,menge:billingQuantity,preisProEinheit:billingRate,stundensatzZugmaschine:isQuantity?0:zugRate,stundensatzAnbaugeraet:anbauRate,maschinenStundensatz:billingRate,maschinenKosten:machineCost,gesamtkosten:totalCost,stundenStart:isQuantity?"":startNumber,stundenEnde:isQuantity?"":endNumber,betriebsstunden:isQuantity?"":Number(gefahreneStunden),fahrerArbeitszeitStunden:Number.isNaN(driverHours)?0:driverHours,diesel:Number.isNaN(dieselNumber)?0:dieselNumber,einsatzart:form.einsatzart,kundeName:form.kundeName.trim(),kundeAdresse:form.kundeAdresse.trim(),kundeOrt:form.kundeOrt.trim(),kundeKontakt:form.kundeKontakt.trim(),bemerkung:form.bemerkung.trim(),erfasstAm:new Date().toISOString()};try{await apiPost(payload);let lsNumber="LS-GESPEICHERT";try{const last=await apiGet("lastDeliveryNote");if(last.ok&&last.lsNumber)lsNumber=last.lsNumber}catch{}setLastDeliveryNote({...previewData,lsNumber});setMessage("Eintrag wurde gespeichert. Der Lieferschein kann jetzt erstellt werden.");setForm({datumVon:today,datumBis:today,fahrer:"",fahrerStundensatz:"",zugmaschine:form.zugmaschine,anbaugeraet:form.anbaugeraet,stundenStart:"",stundenEnde:"",menge:"",diesel:"",einsatzart:"innerbetrieblich",arbeitszeitStunden:"",kundeName:"",kundeAdresse:"",kundeOrt:"",kundeKontakt:"",bemerkung:""});resetStopwatch()}catch{setMessage("Speichern fehlgeschlagen.")}finally{setSending(false)}}
return <div className="page"><main className="app-card"><header className="top"><div className="logo">🚜</div><div className="header-text"><h1>{APP_NAME}</h1><p>Betriebs- und Maschinenerfassung</p></div><button type="button" className="settings-button" onClick={()=>{setCompanyDraft(company);setShowSettings(!showSettings)}}>⚙️</button></header>{showSettings&&<section className="section settings-panel"><h2>Firmendaten Dienstleister</h2><Field label="Firmenname"><input type="text" value={companyDraft.name} onChange={e=>setCompanyDraft({...companyDraft,name:e.target.value})}/></Field><Field label="Zusatz / Ansprechpartner"><input type="text" value={companyDraft.zusatz} onChange={e=>setCompanyDraft({...companyDraft,zusatz:e.target.value})}/></Field><Field label="Adresse"><input type="text" value={companyDraft.adresse} onChange={e=>setCompanyDraft({...companyDraft,adresse:e.target.value})}/></Field><Field label="PLZ / Ort"><input type="text" value={companyDraft.ort} onChange={e=>setCompanyDraft({...companyDraft,ort:e.target.value})}/></Field><Field label="Telefon"><input type="text" value={companyDraft.telefon} onChange={e=>setCompanyDraft({...companyDraft,telefon:e.target.value})}/></Field><Field label="E-Mail"><input type="text" value={companyDraft.email} onChange={e=>setCompanyDraft({...companyDraft,email:e.target.value})}/></Field><Field label="UID / Steuernummer optional"><input type="text" value={companyDraft.uid} onChange={e=>setCompanyDraft({...companyDraft,uid:e.target.value})}/></Field><div className="button-grid"><button type="button" className="primary" onClick={saveCompany} disabled={loading}>Firmendaten speichern</button><button type="button" className="small-button" onClick={()=>setShowSettings(false)}>Schließen</button></div></section>}{message&&<div className="message">{message}</div>}{lastDeliveryNote&&<div className="message action-message"><span>Lieferschein {lastDeliveryNote.lsNumber} bereit.</span><button type="button" className="secondary" onClick={()=>openDeliveryNote(lastDeliveryNote)}>Lieferschein PDF</button></div>}<form onSubmit={saveEntry} className="form"><section className="section"><div className="section-head"><h2>Geräte</h2><button type="button" className="small-button" onClick={()=>setShowManage(!showManage)}>{showManage?"Schließen":"Geräte bearbeiten"}</button></div><Field label="Zugmaschine"><select value={form.zugmaschine} onChange={e=>setForm({...form,zugmaschine:e.target.value,stundenStart:""})} disabled={isQuantity}><option value="">Bitte Zugmaschine auswählen</option>{zugmaschinen.map(i=><option key={i.name} value={i.name}>{i.name} — {formatEuro(i.rate)}/h</option>)}</select></Field><Field label="Anbaugerät"><select value={form.anbaugeraet} onChange={e=>setForm({...form,anbaugeraet:e.target.value,menge:"",stundenStart:"",stundenEnde:""})}><option value="">Kein Anbaugerät</option>{anbaugeraete.map(i=><option key={i.name} value={i.name}>{i.name} — {formatEuro(i.rate)}/{unitLabel(i.unit)}</option>)}</select></Field><div className="info-box">Abrechnung: <strong>{selectedAnbaugeraet?unitName(anbauUnit):"Stunden"}</strong> · Preis: <strong>{formatEuro(billingRate)}/{unitLabel(billingUnit)}</strong></div>{showManage&&<div className="manage-box"><h3>Zugmaschine hinzufügen</h3><div className="add-row"><input type="text" placeholder="z. B. Traktor 1" value={newZugmaschine} onChange={e=>setNewZugmaschine(e.target.value)}/><input type="text" inputMode="decimal" placeholder="€/h" value={newZugRate} onChange={e=>setNewZugRate(e.target.value)}/><button type="button" className="secondary" onClick={addZugmaschine} disabled={loading}>Hinzufügen</button></div><div className="machine-list">{zugmaschinen.map(i=><div className="machine-item" key={i.name}><span>{i.name}<small>{formatEuro(i.rate)} / h</small></span><button type="button" onClick={()=>deleteItem("zugmaschine",i.name)}>Löschen</button></div>)}</div><h3>Anbaugerät hinzufügen</h3><div className="add-row four"><input type="text" placeholder="z. B. Maislegegerät" value={newAnbaugeraet} onChange={e=>setNewAnbaugeraet(e.target.value)}/><select value={newAnbauUnit} onChange={e=>setNewAnbauUnit(e.target.value)}><option value="h">Stunden</option><option value="ha">Hektar</option><option value="m3">m³</option></select><input type="text" inputMode="decimal" placeholder="Preis" value={newAnbauRate} onChange={e=>setNewAnbauRate(e.target.value)}/><button type="button" className="secondary" onClick={addAnbaugeraet} disabled={loading}>Hinzufügen</button></div><div className="machine-list">{anbaugeraete.map(i=><div className="machine-item" key={i.name}><span>{i.name}<small>{formatEuro(i.rate)} / {unitLabel(i.unit)}</small></span><button type="button" onClick={()=>deleteItem("anbaugeraet",i.name)}>Löschen</button></div>)}</div></div>}</section><section className="section"><h2>Kunde / Auftraggeber</h2><Field label="Kunde / Firma"><input type="text" placeholder="z. B. Max Mustermann" value={form.kundeName} onChange={e=>setForm({...form,kundeName:e.target.value})}/></Field><Field label="Adresse"><input type="text" placeholder="Straße und Hausnummer" value={form.kundeAdresse} onChange={e=>setForm({...form,kundeAdresse:e.target.value})}/></Field><Field label="PLZ / Ort"><input type="text" placeholder="PLZ und Ort" value={form.kundeOrt} onChange={e=>setForm({...form,kundeOrt:e.target.value})}/></Field><Field label="Kontakt / Telefon"><input type="text" placeholder="optional" value={form.kundeKontakt} onChange={e=>setForm({...form,kundeKontakt:e.target.value})}/></Field></section><section className="section"><h2>Zeitraum</h2><div className="grid two"><Field label="Von"><input type="date" value={form.datumVon} onChange={e=>setForm({...form,datumVon:e.target.value})}/></Field><Field label="Bis"><input type="date" value={form.datumBis} onChange={e=>setForm({...form,datumBis:e.target.value})}/></Field></div></section><section className="section"><h2>Einsatz</h2><Field label="Fahrer"><input type="text" placeholder="Fahrer eintippen" value={form.fahrer} onChange={e=>setForm({...form,fahrer:e.target.value})}/></Field><Field label="Fahrer-Stundensatz"><input type="text" inputMode="decimal" placeholder="z. B. 45" value={form.fahrerStundensatz} onChange={e=>setForm({...form,fahrerStundensatz:e.target.value})}/></Field><Field label="Einsatzart"><select value={form.einsatzart} onChange={e=>setForm({...form,einsatzart:e.target.value})}><option value="innerbetrieblich">Innerbetrieblich</option><option value="ueberbetrieblich">Überbetrieblich</option></select></Field></section>{isQuantity?<section className="section"><h2>Menge</h2><Field label={`Menge in ${unitLabel(anbauUnit)}`}><input type="text" inputMode="decimal" placeholder={anbauUnit==="ha"?"z. B. 1,5":"z. B. 25"} value={form.menge} onChange={e=>setForm({...form,menge:e.target.value})}/></Field><div className="price-box"><span>Gerätekosten</span><strong>{formatEuro(machineCost)}</strong></div></section>:<section className="section"><h2>Betriebsstunden</h2><button type="button" className="secondary" onClick={takeLastValue} disabled={loading||!form.zugmaschine}>{loading?"Lade...":"Letzten Wert übernehmen"}</button><div className="grid two"><Field label="Stundenzähler Beginn"><input type="text" inputMode="decimal" placeholder="z. B. 1250,5" value={form.stundenStart} onChange={e=>setForm({...form,stundenStart:e.target.value})}/></Field><Field label="Stundenzähler Ende"><input type="text" inputMode="decimal" placeholder="z. B. 1253,0" value={form.stundenEnde} onChange={e=>setForm({...form,stundenEnde:e.target.value})}/></Field></div><div className={!hasValidHours&&form.stundenStart&&form.stundenEnde?"result error":"result"}><span>Stunden</span><strong>{formatHours(gefahreneStunden)}</strong></div><div className="price-box"><span>Maschinenkosten</span><strong>{formatEuro(machineCost)}</strong></div></section>}<section className="section"><h2>Arbeitszeit Fahrer</h2><div className="stopwatch"><strong>{secondsToClock(stopwatchSeconds)}</strong><div className="stopwatch-buttons"><button type="button" className="secondary" onClick={()=>setStopwatchRunning(true)} disabled={stopwatchRunning}>Start</button><button type="button" className="small-button" onClick={()=>setStopwatchRunning(false)} disabled={!stopwatchRunning}>Pause</button><button type="button" className="small-button" onClick={resetStopwatch}>Reset</button></div><button type="button" className="small-button" onClick={useStopwatchAsWorkTime}>Zeit übernehmen</button></div><Field label="Arbeitszeit Fahrer in Stunden"><input type="text" inputMode="decimal" placeholder="z. B. 2,5" value={form.arbeitszeitStunden} onChange={e=>setForm({...form,arbeitszeitStunden:e.target.value})}/></Field><div className="driver-box"><span>Fahrerkosten</span><strong>{formatEuro(driverCost)}</strong></div></section><section className="section"><h2>Gesamt</h2><div className="total-box"><span>Gesamtkosten</span><strong>{formatEuro(totalCost)}</strong></div></section><section className="section"><h2>Diesel & Bemerkung</h2><Field label="Getankte Dieselmenge in Liter"><input type="text" inputMode="decimal" placeholder="z. B. 18,5" value={form.diesel} onChange={e=>setForm({...form,diesel:e.target.value})}/></Field><Field label="Bemerkung / Schäden"><textarea placeholder="z. B. Schaden, Wartung, Besonderheiten" value={form.bemerkung} onChange={e=>setForm({...form,bemerkung:e.target.value})}/></Field></section><div className="button-grid"><button className="primary" type="submit" disabled={!canSave}>{sending?"Speichert...":"Eintrag speichern"}</button><button className="secondary" type="button" onClick={previewDeliveryNote}>Lieferschein Vorschau</button></div></form><footer>© by Steininger Flo</footer></main></div>}
